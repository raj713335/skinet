# skinet


### 2. Run the .NET(5.0) Backend Application:

```
cd skinet
dotnet clean
dotnet build skinet.sln
cd API
dotnet watch run --skinet
```

### 2. Run the .NET(5.0) Backend Application:

```
cd skinet/client
npm install
ng serve
```

